# Micro AWS Website CDN with CDK

This project provides a minimal AWS Website CDN implementation using CDK with a Lambda-backed API Gateway endpoint that serves static website content via CloudFront.
